
public class divisionOfTwoNumbers {//두수의 나눗셈(0레벨)
	public static void main(String[] args) {
		double answer = ((double)3/2)*1000;//문제가 소수점들이 다 나온다음에 곱해야하기에 (double)을 붙여줘서 소수점 끝까지 다 출력하게 해줬다.!!
        System.out.println(answer);
        int result = (int)answer;
        
        System.out.println(result);
	}
}
